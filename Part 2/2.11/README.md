A fresh React app. Nothing too crazy. Can be accessed in ```localhost:5173```.
Note: in the ```package.json```, there is a small change to make it appear in ```localhost:5173```, we add ```--host```.
